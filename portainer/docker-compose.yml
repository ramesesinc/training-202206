version: "3"

services:

   portainer:
      container_name: portainer
      image: portainer/portainer-ce

      restart: always
      logging:
         driver: "json-file"
         options:
            max-file: "5"
            max-size: 10m

      ports:
         - "9000:9000"

      volumes:
         - ./data:/data
